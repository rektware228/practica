<Page x:Class="WpfApp2.Pages.StudentListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentListPage">

    <Grid>
        <StackPanel>
            <ListView x:Name="MyList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding ID_student}" Width="90" Header="ID"></GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Speciality.direction}" Width="280" Header="Имя специальности"></GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding lastname}" Width="100" Header="Фамилия"></GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
